server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /api/ {
        return 200 'API route reached.';
        add_header Content-Type text/plain;
    }
}
